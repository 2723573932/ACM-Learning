#include <bits/stdc++.h>
using namespace std;

void solve()
{
    double p;
    cin >> p;
    cout<<pow(p,4);
}
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    int tt = 1;
    // cin>>tt;
    while (tt--)
        solve();
    return 0;
}
